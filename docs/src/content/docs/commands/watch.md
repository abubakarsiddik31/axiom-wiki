---
title: watch
description: Auto-ingest new files dropped into raw/.
---

```bash
axiom-wiki watch
```

Watches the `raw/` folder and automatically ingests files as they appear. Respects `.axiomignore` patterns, skips already-ingested files, and shows cost per file.

Press `q` to stop watching.
